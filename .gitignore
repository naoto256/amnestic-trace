/target
.DS_Store
.uchgs/
*.swp

# Never committable. Nothing here needs a credential, so anything matching these
# arrived by accident — which is exactly when a backstop earns its keep.
.env
.env.*
*.pem
*.key
*.p12
